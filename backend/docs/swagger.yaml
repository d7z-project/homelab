basePath: /api/v1
info:
  contact: {}
  description: This is a sample server for homelab.
  title: Homelab API
  version: "1.0"
paths:
  /ping:
    get:
      consumes:
      - application/json
      description: get string "pong"
      produces:
      - application/json
      responses:
        "200":
          description: pong
          schema:
            type: string
      summary: Ping the server
      tags:
      - example
swagger: "2.0"
